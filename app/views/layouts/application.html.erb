<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/js/bootstrap.min.js"></script>
    <title>Work And Play - The backgroup music app for your business</title>
  </head>

  <body>
  <nav class="navbar navbar-inverse navbar-static-top">
    <div class="container-fluid">
      <div class="navbar-header">
        <%= link_to("Work And Play", root_path, :class => "navbar-brand") %>
      </div>
      <% if user_logged_in? %>
        <ul class="nav navbar-nav">
          <li><%= link_to("My Locations", locations_path, :class => "navbar-brand") %></li>
          <li><%= link_to("My Music", business_playlists_path(@business, @playlists), :class => "navbar-brand") %></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <li><a href="/logout"><span class="glyphicon glyphicon-log-out"></span>Log Out</a></li>
        </ul>
      <%end %>
    </div>
  </nav>
  <div class="container text-center">
    <%= yield %>
  </div>

  <script type="text/javascript">
    $(document).ready(function(){
      var randomColor1 = Math.floor(Math.random() * 255);
      var randomColor2 = Math.floor(Math.random() * 255);
      var randomColor3 = Math.floor(Math.random() * 255);
      $('h1').on('click', function(){
        $(this).css('color','rgb(' + randomColor1 + ',' + randomColor2 + ',' + randomColor3 + ')');
      })
    })
  </script>
  </body>
</html>
