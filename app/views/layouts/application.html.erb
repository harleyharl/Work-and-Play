<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/js/bootstrap.min.js"></script>
    <title>Work And Play - The backgroup music app for your business</title>
  </head>

  <body>
  <nav class="navbar navbar-inverse navbar-static-top">
    <div class="container-fluid">
      <div class="navbar-header">
        <%= link_to("WorkAndPlay", root_path, :class => "navbar-brand") %>
      </div>
      <% if user_logged_in? %>
      <ul class="nav navbar-nav">
        <li><%= link_to("My Locations", locations_path, :class => "navbar-brand") %></li>
        <li><%= link_to("My Music", business_playlists_path(@business, @playlists), :class => "navbar-brand") %></li>
      </ul>
    <% else %>
      <ul class="nav navbar-nav navbar-right">
        <li><%link_to "Sign Up", :class => "glyphicon glyphicon-user" do %>
          <span>Sign Up&nbsp;</span><%= new_business_path %></li>
          <% end %>
      </ul>
    <%end %>
    </div>
  </nav>
    <%= yield %>
  </body>
</html>
