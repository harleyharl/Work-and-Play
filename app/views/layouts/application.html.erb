<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/js/bootstrap.min.js"></script>
    <title>Work And Play - The backgroup music app for your business</title>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <nav class="navbar navbar-inverse navbar-static-top">
    <div class="container-fluid">
      <div class="navbar-header">
        <%= link_to("Work And Play", root_path, :class => "navbar-brand") %>
      </div>
      <% if user_logged_in? %>
        <ul class="nav navbar-nav">
          <li><%= link_to("My Locations", business_locations_path(@business), :class => "navbar-brand", id: "my-locations") %></li>
          <li><%= link_to("My Music", business_playlists_path(@business), :class => "navbar-brand", id: "my-music") %></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <li><a href="/logout"><span class="glyphicon glyphicon-log-out"></span>Log Out</a></li>
        </ul>
      <%end %>
    </div>
  </nav>
  <div id="app-container" class="container text-center">
    <%= yield %>
  </div>

  <%= javascript_include_tag "application" %>
  </body>
</html>
